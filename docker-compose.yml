version: "2"
services:
  vehicle-db:
    image: gvenzl/oracle-xe:slim
    ports:
    - 1521:1521
    environment:
      ORACLE_DATABASE: vehicle
      ORACLE_RANDOM_PASSWORD: "true"
      APP_USER: scott
      APP_USER_PASSWORD: tiger